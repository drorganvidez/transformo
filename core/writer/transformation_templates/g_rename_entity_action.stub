
    <transformation type="{{ action.transformation_type()}}" id="{{ action.__class__.__name__ }}">
        <action type="{{ action.action_type()}}">
            <entity>{{ action.entity().id() }}</entity>
            <rename>{{ action.rename() }}</rename>
        </action>
    </transformation>
