-- -----------------------------------------------------
-- Transformation  {{ transformation_name }}
-- -----------------------------------------------------

ALTER TABLE `{{ database_name_to }}`.`{{ action.entity().id() }}`
  ADD COLUMN `{{ action.attribute() }}` {{ action.type().upper() }};